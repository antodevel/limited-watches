import { timeline } from '@/data/timeline';
export function OrbitalTimeline() {
  return <section id="history" className="history-section section-pad" aria-labelledby="history-title"><div className="section-heading"><p className="eyebrow">01 / ПРИТЯЖЕНИЕ КОСМОСА</p><h2 id="history-title">У каждой орбиты<br />есть начало.</h2><p className="section-intro">Не просто отсчитывать мгновения.<br />Хранить то, что больше времени.</p></div><div className="timeline"><span className="timeline-mobile-line" aria-hidden="true" /><svg className="timeline-orbit" viewBox="0 0 1000 1500" preserveAspectRatio="none" aria-hidden="true"><path className="path-track" d="M500 0C500 110 730 40 730 250S300 540 300 750S730 1040 730 1250S500 1410 500 1500"/><path className="path-draw" d="M500 0C500 110 730 40 730 250S300 540 300 750S730 1040 730 1250S500 1410 500 1500"/></svg>{timeline.map((event, index) => <article className={`timeline-entry timeline-entry-${index}`} key={event.id}><div className="timeline-node"><span /><span className="eyebrow">{event.id}</span></div><div className="timeline-copy"><span className="eyebrow">{event.id} / {event.label}</span><h3>{event.title.split('\n').map((line, i) => <span key={line}>{i > 0 && <br />}{line}</span>)}</h3><p>{event.text}</p></div><span className="timeline-ghost" aria-hidden="true">{event.id}</span></article>)}</div><div className="history-end" aria-hidden="true"><span />КОСМОС<span /></div></section>;
}
